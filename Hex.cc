#include "HexPlayer.cc"

using namespace std;

int main()
{
  srand(time(nullptr));
  int board_size;
  cout<<"Board Size: ";
  cin>>board_size;
  HexBoard H(board_size);
  char b, r;
  cout<<"Enter H for Human and C for Computer"
  cout<<"BLUE Player: ";
  cin>>b;
  cout<<"RED Player: ";
  cin>>r;
  if((b == 'C' || b == 'c) && (r == 'C' || r == 'c')) {
    HexBoard h1
  }

}
